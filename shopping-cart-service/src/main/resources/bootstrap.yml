server:
  port: 8082

spring:
  application:
    name: shopping-cart-service

  cloud:
    config:
      uri: ${CONFIG_URI:http://localhost:8888}
      fail-fast: true
      username: config
      password: ${CONFIG_PASSWORD}


