server:
  port: 8083

spring:
  application:
    name: order-service

  cloud:
    config:
      uri: ${CONFIG_URI:http://localhost:8888}
      fail-fast: true
      username: ${CONFIG_USER}
      password: ${CONFIG_PASSWORD}


