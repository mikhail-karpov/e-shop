spring:
  application:
    name: config

  cloud:
    config:
      server:
        native:
          search-locations: classpath:/shared

  profiles:
    active: native

  security:
    user:
      name: config
      password: ${CONFIG_PASSWORD}

server:
  port: 8888
