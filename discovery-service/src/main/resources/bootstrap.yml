spring:
  application:
    name: discovery-service

  cloud:
    config:
      uri: http://config:8888
      fail-fast: true

eureka:
  client:
    register-with-eureka: false
    fetch-registry: false
